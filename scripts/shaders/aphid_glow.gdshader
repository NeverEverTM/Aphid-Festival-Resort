shader_type canvas_item;
render_mode unshaded;

void fragment() {
    //vec2 uv = UV - vec2(test);
	//
	//if (COLOR.a == 0.0 && texture(TEXTURE, uv).a == 0.0)
	//{
    	//COLOR = color;
		//COLOR.a = 0.5;
	//}
}