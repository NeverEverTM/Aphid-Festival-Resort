Creating a structure is a simple as implementing an item, both support '.png' and '.tres' icons to be loaded, and while items can have an *optional* custom item scene, structures require it.

Here is a few things to consider when adding a structure:
- 